# 합동작업 v1.md

## 목적
v1.0부터의 개발을 SDD(스펙) 기반으로 진행하기 위한 **공동 작업 로그 + 결정 기록**.

- 이 문서는 Claude 등 다른 에이전트가 “학습/참조”할 수 있는 형태로 유지한다.
- v1.0의 **단일 진실(SSOT)** 은 `docs/sdd/v1.0/*` 문서다.

## 헌법 규칙 (v1.0)
1. v1.0 코드 변경 전: 관련 SDD가 존재하고 합의되어야 한다.
2. v1.0 코드 변경 시: 구현이 스펙에서 벗어나면 코드를 바꾸기 전에 스펙을 먼저 수정한다.
3. v1.0 작업 기록: 모든 작업은 `codex.md` + 이 파일(`합동작업 v1.md`)에 남긴다.
4. 스펙 문서: 최종 스펙은 `docs/sdd/v1.0/*`에만 기록한다(여긴 로그/결정/요약).

## 링크 (SSOT)
- v1.0 SDD 루트: `docs/sdd/v1.0/README.md`
- v0 미구현 백로그(입력 받는 곳): `docs/sdd/v1.0/99-v0-gap-backlog.md`
- 프로젝트 정의(타임존/매출/상태 등): `docs/sdd/v1.0/00-project.md`

## 결정 로그 (Decision Log)
> 형식: YYYY-MM-DD - 결정 - 근거/영향

- 2026-02-14: v0는 “기존 구현”으로 고정하고, v1.0부터는 SDD 선행 원칙으로 개발한다.

## 작업 기록

### 2026-02-14 기록 1 (v1.0 문서 스캐폴딩)
- 작업: v1.0 SDD 문서 구조(SSOT) 생성
- 변경 파일:
  - `docs/sdd/v1.0/README.md`
  - `docs/sdd/v1.0/00-project.md`
  - `docs/sdd/v1.0/99-v0-gap-backlog.md`
  - `docs/sdd/v1.0/_templates/page-sdd.md`
  - `docs/sdd/v1.0/01-admin/README.md`
  - `docs/sdd/v1.0/01-admin/permissions-matrix.md`
  - `docs/sdd/v1.0/01-admin/admin-dashboard.md`
  - `docs/sdd/v1.0/01-admin/admin-tee-times.md`
- 결과:
  - v1.0 개발은 문서 기반으로만 진행 가능한 틀 확보
- 남은 이슈:
  - `docs/sdd/v1.0/00-project.md`의 핵심 정의(타임존/매출/상태/서비스키 정책) 확정 필요

### 2026-02-14 기록 2 (MCP 추가: sequential-thinking)
- 작업:
  - Claude/Codex 공동작업 보조용 MCP 서버 `sequential-thinking` 추가
- 수행 명령:
  - `codex mcp add sequential-thinking npx -y @modelcontextprotocol/server-sequential-thinking`
- 결과:
  - 글로벌 MCP 서버로 등록 완료(`enabled: true`, `transport: stdio`)
  - 확인 명령:
    - `codex mcp list`
    - `codex mcp get sequential-thinking`
- 참고:
  - 공식 서버 문서: `modelcontextprotocol/servers`의 `src/sequentialthinking`

### 2026-02-14 기록 3 (VSCode MCP 추가: testsprite)
- 작업:
  - 사용자 요청으로 VSCode용 MCP 서버 `testsprite` 설정 추가
- 변경 파일:
  - `.vscode/mcp.json`
- 결과:
  - workspace MCP 설정 파일 생성 완료
  - 서버 실행 방식: `npx -y @testsprite/testsprite-mcp@latest`
  - 보안상 API 키 평문 저장 없이 VSCode 입력 프롬프트(`promptString`, password=true) 방식으로 구성
- 남은 이슈:
  - VSCode에서 워크스페이스를 다시 열고 MCP 서버 시작 시 API 키 입력 필요

### 2026-02-16 기록 4 (v1.0 우선순위/프라이싱 스펙 확정 반영)
- 작업:
  - 오너 요청사항 기반으로 v1.0 우선순위를 문서에 고정
  - 프라이싱 엔진 Step 0~3 스펙 문서화
  - Admin 티타임 DB 반영 요구사항을 SDD/백로그에 반영
- 변경 파일:
  - `docs/sdd/v1.0/README.md`
  - `docs/sdd/v1.0/00-project.md`
  - `docs/sdd/v1.0/02-pricing-engine-v1.md`
  - `docs/sdd/v1.0/V1-PRIORITY-UPDATE.md`
  - `docs/sdd/v1.0/99-v0-gap-backlog.md`
  - `docs/sdd/v1.0/01-admin/README.md`
  - `docs/sdd/v1.0/01-admin/admin-crawler.md`
  - `docs/sdd/v1.0/01-admin/admin-tee-times.md`
- 결과:
  - v1.0 최우선 과제를 "프라이싱 엔진"으로 고정
  - `/admin/crawler` 카테고리 분류, 4시간 수집, 메모, 엔진 연동 요구사항 반영 완료
  - `/admin/tee-times`의 "추가/수정 DB 즉시 반영" AC를 P0로 명시
- 남은 이슈:
  - 매출 집계 공식(환불 반영 상세), 프라이싱 factors 가중치 상세 규칙은 다음 스펙 라운드에서 수치 확정 필요

### 2026-02-17 기록 5 (v1.0 실행 계획 수립 - Claude)
- 작업:
  - v0 코드 전체 분석 (크롤러/티타임/가격엔진/API/DB)
  - v1.0 마스터플랜 작성 (`V1-MASTER-PLAN.md`)
  - v1.0 실행 계획 작성 (`V1-EXECUTION-PLAN.md`) - Codex 지시용
- 변경 파일:
  - `docs/sdd/v1.0/V1-MASTER-PLAN.md` (신규)
  - `docs/sdd/v1.0/V1-EXECUTION-PLAN.md` (신규)
- 분석 결과:
  - 프라이싱 엔진: V1 작동 중, marketReference 조회만 하고 계산에는 미반영
  - 크롤러: 3개 테이블 존재, snapshot에 note 필드 없음, 스케줄러 없음
  - 티타임 Admin: Server Action CRUD 있으나 API Route는 PATCH만
  - 외부 시장가→가격 계산 연동 미구현
- 실행 계획 요약:
  - Priority 1: 프라이싱 엔진 (Step 0~3, 10개 세부 작업)
  - Priority 2: Admin 티타임 DB 연동 (Step A~C, 3개 세부 작업)
  - 총 13개 Codex 지시 단위로 분할
- 결정:
  - 2026-02-17: 시장가 반영 방식 = "basePrice와 marketPrice 차이의 50% 수렴" 방식 채택
  - 2026-02-17: 시즌 판단 = 태그 우선, 없으면 월/요일 기반 자동 판단
  - 2026-02-17: 티타임 API = Server Action → API Route 전환 (검증/에러 일관성)
- 남은 이슈:
  - 시장가 수렴 비율(50%) 실제 운영 후 튜닝 필요
  - 크롤러 adapter_code별 수집 로직 구현 세부사항 미정
  - 성수기/비수기 월별 기준 오너 확인 필요

### 2026-02-17 기록 6 (V1-EXECUTION-PLAN.md 상세 구현 지시서 전면 재작성 - Claude)
- 작업:
  - 핵심 소스 파일 7개 + auth/lib 3개 정밀 분석 (라인 단위)
  - `V1-EXECUTION-PLAN.md`를 ~2625줄 상세 구현 지시서로 전면 재작성
  - Codex가 한 줄도 빠짐없이 따라할 수 있는 수준으로 모든 변경 사항 명시
- 분석한 소스 파일:
  - `utils/pricingEngine.ts` (244줄) - 현행 V1 가격 엔진
  - `app/api/pricing/route.ts` (265줄) - 가격 API
  - `components/admin/CrawlerMonitorClient.tsx` (442줄) - 크롤러 UI
  - `app/admin/crawler/page.tsx` (347줄) - 크롤러 서버 컴포넌트
  - `app/admin/tee-times/page.tsx` (570줄) - 티타임 관리 UI
  - `app/admin/tee-times/actions.ts` (398줄) - 서버 액션
  - `app/api/admin/tee-times/route.ts` (86줄) - 티타임 API
  - `lib/auth/getCurrentUserWithRoles.ts` (399줄) - 인증/권한
  - `lib/supabase/admin.ts` (37줄) - 어드민 클라이언트
  - `lib/supabase/server.ts` (75줄) - 서버 클라이언트
- 변경 파일:
  - `docs/sdd/v1.0/V1-EXECUTION-PLAN.md` (전면 재작성, ~2625줄)
- 지시서 포함 내용:
  - 13개 Codex 작업 단위 (S0, S1-A~C, S2-A~C, S3-A~C, TA~TC)별 상세 구현 스펙
  - 모든 변경에 대해 정확한 라인 번호, before/after 코드 블록 명시
  - 신규 파일 6개의 완전한 TypeScript 코드 포함
  - SQL 마이그레이션 (`20260218100000_snapshot_notes.sql`)
  - Vercel Cron 설정 (`vercel.json`)
  - 7개 신규 프라이싱 팩터 설계: MARKET_PRICE, PEAK_SEASON, OFF_SEASON, WEEKDAY, TEMPERATURE, WIND, LOW_DEMAND
  - Auth 패턴 (`requireAdminAccess()`) 및 API 응답 패턴 문서화
  - UI 컴포넌트 스펙: FilterBar, SnapshotTable, CrawlStatusPanel, InlineNoteEditor
  - 각 단위별 AC(수락 조건) 체크리스트
  - 의존성 그래프 및 병렬 실행 전략
- 결정:
  - 2026-02-17: 7개 신규 팩터 가중치 확정 (MARKET_PRICE ±20%, PEAK_SEASON -50%할인, OFF_SEASON +5%, WEEKDAY +3%, TEMPERATURE +5%, WIND +5%, LOW_DEMAND +3%)
  - 2026-02-17: 한국 공휴일 2026년 하드코딩 배열 방식 채택
  - 2026-02-17: pricing/route.ts 코드 재배치 필요 확인 (results map이 marketSnapshotByKey 빌드 전에 실행되는 버그)
  - 2026-02-17: Server Action 중 `getAccessibleGolfClubs`만 유지, 나머지 티타임 CRUD는 API Route로 전환
- 남은 이슈:
  - Codex 실행 시작 대기 중 (사용자가 어떤 작업 단위부터 시작할지 결정 필요)
  - weather_cache 테이블의 `tmp` 필드 존재 여부 미확인 (graceful fallback 적용)
  - 크롤러 adapter_code별 실제 수집 로직 구현은 별도 작업

### 2026-02-19 기록 7 (GolfRock 크롤러 어댑터 리팩터 + Codex 지시서 - Claude)
- 작업:
  - GolfRock 사이트 Playwright headful 테스트 (로그인 폼 구조 + 가격 HTML 구조 분석)
  - `crawlGolfRockList()` 전면 리팩터: 로그인 자동화 + 아코디언 AJAX 기반 가격 수집
  - `loginGolfRock()` 헬퍼 함수 신규 추가
  - Codex용 크롤러 나노 단위 지시서 작성 (`CRAWLER-CODEX-INSTRUCTIONS.md`)
- 테스트 결과:
  - GolfRock 로그인: `memb_handphone` + `memb_pass` → `button.btn-yell` → 성공
  - 아코디언 클릭 → `join_time_list_get.asp` AJAX → `<p class='price'>현장70,000원</p>` 파싱
  - dry-run: **472행 수집 성공** (30+ 골프장, 가격 13,000원~130,000원)
- 변경 파일:
  - `crawler/src/crawl-final-prices.mjs` (loginGolfRock 추가, crawlGolfRockList 리팩터, crawlTarget args 전달)
  - `crawler/src/test-golfrock-login.mjs` (임시, C3에서 삭제 예정)
  - `crawler/src/test-golfrock-deep.mjs` (임시, C3에서 삭제 예정)
  - `docs/sdd/v1.0/CRAWLER-CODEX-INSTRUCTIONS.md` (신규)
- 결정:
  - 2026-02-19: Chrome 확장프로그램 대신 Playwright 자동화 채택 (GitHub Actions 4시간 cron 활용)
  - 2026-02-19: GolfRock 자격증명은 환경변수 (`GOLFROCK_LOGIN_ID`, `GOLFROCK_LOGIN_PW`)로만 관리
- 남은 이슈 (Codex 작업):
  - C1: GitHub Actions 워크플로우에 GOLFROCK env vars 추가
  - C2: TeeupNJoy club ID discovery 실행
  - C3: 테스트 스크립트 삭제
  - C4: 실 데이터 크롤링 전체 window 실행 + 검증
  - C5: Admin 크롤러 UI 데이터 확인

### 2026-02-19 기록 8 (GolfRock 크롤러 직접 AJAX 방식 개선 - Claude)
- 작업:
  - 기존 아코디언 클릭 방식 → 직접 AJAX fetch 방식으로 전면 개선
  - AJAX 엔드포인트 파라미터 역공학 (request interception으로 정확한 POST body 캡처)
  - `parseGolfRockPriceHtml()` Node.js regex 기반 HTML 파서 신규 추가
  - `crawlGolfRockList()` 재작성: `#tranForm` 직렬화 → `join_time_list_get.asp` POST → 5개 병렬 배치
  - `CRAWLER-CODEX-INSTRUCTIONS.md` 전면 업데이트 (직접 AJAX 방식 반영)
- 분석 결과:
  - AJAX 엔드포인트: `POST join_time_list_get.asp`
  - 핵심 파라미터: `club_code_str` (= `.club_info[data-value]`), `cond_date` (= YYYYMMDD)
  - 폼: `#tranForm` 직렬화 (latitude, longitude, cond_area, 필터 등 포함)
  - 사이트 JS 함수: `fnv_compare_time_list_get(v_club_data)` → `$(#tranForm).serialize()` POST
  - 응답: `<ul class='listwrap'>` 내 `<p class='price'><span>현장</span>80,000원</p>` 구조
- 성능 비교:
  - 이전 (아코디언 클릭): ~4분+ (순차 클릭, 300ms 대기, CSS 셀렉터 의존)
  - 개선 (직접 AJAX): **18초** (5개 병렬 배치, 폼 직렬화, regex 파싱) — **13배 빠름**
- 테스트 결과:
  - WEEK_BEFORE 단일: 10행, 13초, 가격 35,000~120,000원
  - 4개 window 전체: 24행, 18초, 실제 가격 수집 + 양도 테이블 파싱
  - 골프장: 정산, 파크밸리, 더크로스비, 동서울, 골프존_천안, 클럽72, 동촌, 떼제베 등
- 변경 파일:
  - `crawler/src/crawl-final-prices.mjs` (parseGolfRockPriceHtml 추가, crawlGolfRockList 직접 AJAX 재작성)
  - `crawler/src/test-golfrock-ajax-params.mjs` (임시, C3에서 삭제 예정)
  - `crawler/src/test-golfrock-html-sample.mjs` (임시, C3에서 삭제 예정)
  - `crawler/.env.local` (GOLFROCK 자격증명 추가)
  - `docs/sdd/v1.0/CRAWLER-CODEX-INSTRUCTIONS.md` (직접 AJAX 방식 반영 업데이트)
- 결정:
  - 2026-02-19: 아코디언 클릭 방식 폐기, 직접 AJAX fetch 방식 최종 채택
  - 2026-02-19: `#tranForm` 폼 직렬화를 기본 파라미터로 사용 (사이트 JS 동작과 동일)
  - 2026-02-19: 가격 없는 항목 ("현장그린피") 필터링 — `hasPrice` 플래그로 처리
- 남은 이슈 (Codex 작업, 변경 없음):
  - C1: GitHub Actions 워크플로우에 GOLFROCK env vars 추가
  - C2: TeeupNJoy club ID discovery 실행
  - C3: 테스트 스크립트 삭제 (4개: login, deep, ajax-params, html-sample)
  - C4: 실 데이터 크롤링 전체 window 실행 + 검증
  - C5: Admin 크롤러 UI 데이터 확인

### 2026-02-19 기록 9 (섹터별 사전 설계 게이트 규칙 고정 - Codex)
- 작업:
  - 사용자 지시에 따라 “각 섹터 작업 전 설계 완료” 규칙을 v1.0 문서에 강제 반영
- 변경 파일:
  - `docs/sdd/v1.0/README.md`
  - `docs/sdd/v1.0/SECTOR-WORKFLOW.md` (신규)
  - `docs/sdd/v1.0/_templates/sector-design-gate.md` (신규)
  - `docs/sdd/v1.0/V1-PRIORITY-UPDATE.md`
- 결과:
  - 섹터별 구현 전 필수 게이트(범위/데이터/API/권한/실패케이스/QA/롤백/승인) 명문화
  - 승인 전 구현 금지 원칙 문서화 완료

### 2026-02-19 기록 10 (Pricing/Crawler 섹터 착수 - C1/C3 완료)
- 작업:
  - 섹터 게이트 문서 생성 및 승인 상태로 착수
  - C1: GitHub Actions crawler env에 GOLFROCK 시크릿 변수 반영
  - C3: GolfRock 임시 테스트 스크립트 4개 정리(삭제)
  - C2: Teeup discovery 실행 시도
- 변경 파일:
  - `docs/sdd/v1.0/sectors/pricing-crawler-gate.md` (신규)
  - `.github/workflows/crawler-ingest.yml`
  - `crawler/src/test-golfrock-login.mjs` (삭제)
  - `crawler/src/test-golfrock-deep.mjs` (삭제)
  - `crawler/src/test-golfrock-ajax-params.mjs` (삭제)
  - `crawler/src/test-golfrock-html-sample.mjs` (삭제)
  - `docs/sdd/v1.0/README.md` (sectors 경로 맵 추가)
- 검증:
  - `npm --prefix crawler run check` 통과
- 블로커:
  - C2 실행 시 Playwright Chromium 프로세스가 `SIGTRAP`으로 종료되어 discovery 완료 불가
  - 에러 요약: `browserType.launch: ... process did exit: signal=SIGTRAP`

### 2026-02-19 기록 11 (Admin/Tee-Times 섹터 착수 - TA/TB 구현)
- 작업:
  - Admin 티타임 섹터 게이트 문서 작성 후 API/프론트 연동 구현
  - TA: 티타임 API 확장 (`GET/POST`, `PATCH` action 호환 유지)
  - TA-2: `/:id` 전용 `PATCH/DELETE` 라우트 추가
  - TB: `/admin/tee-times`에서 CRUD를 API 호출 방식으로 전환(클럽 목록은 Server Action 유지)
- 변경 파일:
  - `docs/sdd/v1.0/sectors/admin-tee-times-gate.md` (신규)
  - `app/api/admin/tee-times/route.ts`
  - `app/api/admin/tee-times/[id]/route.ts` (신규)
  - `app/admin/tee-times/page.tsx`
  - `docs/sdd/v1.0/01-admin/admin-tee-times.md`
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과 (신규 route `/api/admin/tee-times/[id]` 포함)
- 결과:
  - BOOKED 항목 수정 시 서버에서 `409 CONFLICT` 반환하도록 강제
  - CLUB_ADMIN 접근 범위를 clubId 기준으로 서버 검증
  - 화면 에러 처리를 `alert` 중심에서 에러 배너 표시 방식으로 일부 전환

### 2026-02-19 기록 12 (Pricing/Crawler C2 해소 + C4 완료)
- 작업:
  - C2 블로커(`Playwright SIGTRAP`) 해소를 위해 Teeup discovery를 direct HTTP 방식으로 전환
  - C4 4개 window 실수집 재실행 및 DB 적재 검증
  - pricing-crawler 섹터 게이트 AC/체크리스트를 실제 결과로 업데이트
- 변경 파일:
  - `crawler/src/discover-teeup-club-ids.mjs`
  - `docs/sdd/v1.0/sectors/pricing-crawler-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - teeup target parser_config 확인: `club_ids_count=14`
  - window 실행 로그:
    - `WEEK_BEFORE` rows=17
    - `TWO_DAYS_BEFORE` rows=20
    - `SAME_DAY_MORNING` rows=2
    - `IMMINENT_3H` rows=6
  - Supabase SQL 조회로 최근 1시간 snapshots 적재 확인
- 결과:
  - Pricing/Crawler 섹터는 C1~C4 완료, C5(UI 확인)만 남음
  - `report-snapshot-health.mjs`는 현재 환경 `fetch failed`로 실패하여 SQL 대체 검증 기준을 문서에 반영

### 2026-02-19 기록 13 (Pricing/Crawler C5 완료 + DEMO_MODE 인증 안정화)
- 작업:
  - C5(`/admin/crawler` UI 확인) 수행
  - DEMO 모드에서 `/admin/crawler`가 307로 리다이렉트되던 인증 경로 문제 수정
- 변경 파일:
  - `lib/auth/getCurrentUserWithRoles.ts`
  - `docs/sdd/v1.0/sectors/pricing-crawler-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `NEXT_PUBLIC_DEMO_MODE=true` 환경에서 `/admin/crawler` 200 응답 확인
  - 페이지 핵심 UI 텍스트/지표 렌더링 확인(크롤링 모니터링, 최근 30일, 골프장 수, 스냅샷 수)
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - Pricing/Crawler 섹터 C1~C5 완료
  - 게이트 상태를 Completed로 갱신

### 2026-02-19 기록 14 (Admin/Tee-Times 수동 QA 완료 + 버그 보완)
- 작업:
  - Admin/Tee-Times 섹터의 수동 QA 체크리스트 완료
  - QA 중 발견된 API 생성/에러 처리 버그 수정
- 변경 파일:
  - `app/api/admin/tee-times/route.ts`
  - `app/api/admin/tee-times/[id]/route.ts`
  - `lib/auth/getCurrentUserWithRoles.ts`
  - `docs/sdd/v1.0/sectors/admin-tee-times-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - API 시나리오:
    - GET(200), POST(200), PATCH `:id`(200), PATCH action BLOCKED/OPEN(200)
    - DELETE BLOCKED(409), DELETE OPEN(200), 재조회 시 삭제 반영 확인
  - BOOKED 수정 검증:
    - 테스트 row BOOKED 전환 후 PATCH `:id` = 409 CONFLICT
  - `/admin/tee-times` 200 응답 확인
- 결과:
  - Admin/Tee-Times 섹터 게이트를 Completed로 업데이트
  - 생성 API의 `current_price` 누락 문제 해결
  - fallback demo user 환경에서 `updated_by` 쓰기 실패 방어 추가

### 2026-02-19 기록 15 (Admin/Dashboard 섹터 구현 완료 - D1~D3)
- 작업:
  - Admin Dashboard 섹터 설계 게이트 작성 후 D1~D3 구현
  - D1: 최근 14일(KST) 매출 시계열 + `ok/empty/error` 상태 반영
  - D2: AI Pricing Engine 카드 실계산 상태 반영(샘플 티타임 기반)
  - D3: 대시보드 티타임 PATCH 실패 메시지 파싱 개선
- 변경 파일:
  - `docs/sdd/v1.0/sectors/admin-dashboard-gate.md`
  - `app/admin/page.tsx`
  - `components/AdminDashboardNew.tsx`
  - `docs/sdd/v1.0/01-admin/admin-dashboard.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `/admin` 200 응답 확인
  - 렌더링 텍스트 확인: `일별 매출 추이 (최근 14일)`, AI 샘플 상태/ID 문구
  - PATCH validation 에러 메시지 확인: `id는 양의 정수여야 합니다.`
- 결과:
  - Admin/Dashboard 섹터 게이트 Completed
  - 대시보드의 매출 추이/엔진 상태 신뢰도 개선 반영 완료

### 2026-02-19 기록 16 (DEMO 사용자 조회 안정화)
- 작업:
  - DEMO 사용자 조회 시 fallback으로 떨어지던 원인 컬럼 선택 오류 수정
- 변경 파일:
  - `lib/auth/getCurrentUserWithRoles.ts`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - DEMO 모드 `/admin` 200 응답 확인
  - 로그 확인: `superadmin@tugol.dev` 정상 매칭, fallback 경고 미발생
  - `npm run lint` / `npm run build` 통과
- 결과:
  - DEMO 기반 관리자 수동 QA 신뢰도 향상

### 2026-02-19 기록 17 (Pricing Engine Core 섹터 완료)
- 작업:
  - `/api/pricing` market snapshot 조회를 admin client optional로 보완
  - Pricing Engine Core 게이트 AC/수동 QA 체크리스트 완료 업데이트
- 변경 파일:
  - `app/api/pricing/route.ts`
  - `docs/sdd/v1.0/sectors/pricing-engine-core-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - 임시 snapshot(`source_url=qa://codex-market-price-ac-20260219`) 삽입 후
    `/api/pricing?golfClubId=1&date=2026-01-17&limit=3`에서
    `MARKET_PRICE` factor + `deltaFromMarket=10000` 확인
  - 임시 snapshot 삭제 후 동일 API 재조회에서 `snapshotKeys=0`,
    `MARKET_PRICE` 미노출 확인
- 결과:
  - Pricing Engine Core 섹터 게이트 Completed
  - market snapshot 미존재 시에도 `/api/pricing` 정상 응답(`status=success`) 유지 확인

### 2026-02-19 기록 18 (Pricing/Crawler Step2 메모·특성 저장 완료)
- 작업:
  - Pricing/Crawler Step2 섹터 설계 게이트 작성 후 구현
  - `GET/PATCH /api/admin/crawler/snapshots` API 추가
  - `/admin/crawler`에 가격 행 메모/특성 저장 UI 추가
  - 메모 저장 구조를 `external_price_snapshots.payload.manual_note`로 표준화
- 변경 파일:
  - `docs/sdd/v1.0/sectors/pricing-crawler-notes-gate.md`
  - `app/api/admin/crawler/snapshots/route.ts`
  - `components/admin/CrawlerMonitorClient.tsx`
  - `docs/sdd/v1.0/01-admin/admin-crawler.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - DEMO 모드:
    - `GET /api/admin/crawler/snapshots?courseName=포천힐스&limit=3` -> 200
    - `PATCH /api/admin/crawler/snapshots` note/traits 저장 -> 200
    - GET 재조회 시 `manualNote` 메타(updatedByEmail/updatedAt) 반영 확인
    - 빈 note+traits 저장 시 `manualNote=null` 확인
    - 잘못된 traits 타입 요청 -> 400
  - non-demo(비로그인):
    - 동일 GET 요청 -> 401
  - `npm run lint` / `npm run build` 통과
- 결과:
  - Pricing/Crawler Step2 섹터 게이트 Completed (N1~N4)
  - v1 요구사항의 "가격정보 옆 간단 메모/특성 저장" 기능 운영 가능 상태 확보

### 2026-02-19 기록 19 (Pricing Engine Step3 traits 연동 완료)
- 작업:
  - Step3 섹터 게이트 작성 후 엔진 연동 구현
  - `utils/pricingEngine.ts`에 `marketTraits` 입력 및 `TRAIT_*` factor 추가
  - `/api/pricing`에서 snapshot `payload.manual_note` 파싱 후 엔진에 전달
  - `marketReference`에 `manualNote/manualTraits/manualNoteUpdatedAt/manualNoteUpdatedByEmail` 노출
- 변경 파일:
  - `docs/sdd/v1.0/sectors/pricing-engine-traits-gate.md`
  - `utils/pricingEngine.ts`
  - `app/api/pricing/route.ts`
  - `docs/sdd/v1.0/02-pricing-engine-v1.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - 임시 snapshot traits(`비수기`,`이벤트`,`저수요`) 삽입 후
    `/api/pricing?golfClubId=1&date=2026-01-17&limit=1`에서
    `TRAIT_OFF_SEASON`, `TRAIT_EVENT`, `TRAIT_LOW_DEMAND` 노출 확인
  - 같은 row를 `성수기` trait로 변경 후 `TRAIT_PEAK_SEASON` 노출 확인
  - 임시 row 삭제 후 traits factor 미노출 확인
  - payload가 비정상(string JSON)이어도 `/api/pricing` `status=success` 확인 후 정리
- 결과:
  - Pricing Engine Step3 섹터 게이트 Completed (T1~T4)
  - 메모/특성 기반 가격판단이 실제 응답 factors로 검증 가능 상태 확보

### 2026-02-19 기록 20 (Admin Reservations 하드닝)
- 작업:
  - `/admin/reservations` 및 `/admin/reservations/[id]`에 admin 가드 적용
  - 페이지 데이터 경로를 admin client optional 우선 + server client fallback으로 통일
  - `app/admin/actions.ts` 전역 anon fallback client 제거
  - 예약 status 필터 타입가드 추가로 타입 안정화
- 변경 파일:
  - `docs/sdd/v1.0/sectors/admin-reservations-hardening-gate.md`
  - `app/admin/reservations/page.tsx`
  - `app/admin/reservations/[id]/page.tsx`
  - `app/admin/actions.ts`
  - `docs/sdd/v1.0/01-admin/admin-reservations.md`
  - `docs/sdd/v1.0/01-admin/README.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과
  - 런타임 HTTP 수동검증은 sandbox `listen EPERM` 제약으로 부분 보류
- 결과:
  - Admin Reservations 섹터의 인증/데이터 경로가 v1 공통 규칙에 맞게 정렬됨
  - 게이트 문서 상태: `Completed (Implementation Done, Runtime QA Partially Blocked)`

## v0 미구현/불완전 인테이크
v0에서 “안 된 것”은 아래 포맷으로 받아서 `docs/sdd/v1.0/99-v0-gap-backlog.md`에 정리한다.

```text
[ID] (예: ADMIN-001)
- 위치(Route/화면):
- 현재 문제(As-Is):
- 목표 동작(To-Be):
- 중요도(P0/P1/P2):
- 영향(사용자/매출/운영):
- 관련 데이터(테이블/필드):
- 권한(누가 할 수 있어야 하는가):
- 완료 조건(AC):
- 메모/스크린샷/로그:
```

### 2026-02-19 기록 21 (Admin No-Show 섹터 완료 + 레거시 reservations 스키마 대응)
- 작업:
  - `/api/admin/no-show` 500 원인 분석 및 DB 스키마 검증
  - `reservations.status` 미존재 환경(legacy `payment_status`) 자동 감지 로직 추가
  - GET은 레거시/신규 스키마 모두 조회 가능하도록 보강
  - POST는 legacy 스키마에서 409 가이드 에러 반환(무의미한 실패 대신 명확한 운영 메시지)
  - no-show 섹터 게이트 문서 Completed 처리
- 변경 파일:
  - `app/api/admin/no-show/route.ts`
  - `docs/sdd/v1.0/sectors/admin-no-show-gate.md`
  - `docs/sdd/v1.0/01-admin/admin-no-show.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `/admin/no-show` 200
  - `GET /api/admin/no-show?date=2026-02-19` -> 200
  - `POST /api/admin/no-show` `{}` -> 400
  - `POST /api/admin/no-show` `{reservationId:"test-id"}` -> 409(legacy schema 가이드)
  - `npm run lint` / `npm run build` 통과
- 결과:
  - Admin No-Show 섹터를 계획/구현/검증/문서화까지 완료
  - 다음 우선순위는 reservations v1 스키마 정합성 복구(마이그레이션)

### 2026-02-19 기록 22 (Admin Reservations 스키마 호환 섹터 완료)
- 작업:
  - admin 예약/통계에서 `reservations.status` 고정 의존 제거
  - status 자동 감지(`status`/`payment_status`) 유틸 추가
  - canonical reservation status 변환 유틸 추가
  - `/admin/reservations` 목록/필터, `/admin/reservations/[id]` 상세, `getAdminDashboardStats` 액션 호환화
  - no-show 관련 서버 액션도 legacy 스키마에서 명확한 에러를 반환하도록 정리
- 변경 파일:
  - `lib/reservations/statusColumn.ts`
  - `utils/reservationStatus.ts`
  - `app/admin/reservations/page.tsx`
  - `app/admin/reservations/[id]/page.tsx`
  - `app/admin/actions.ts`
  - `app/api/admin/no-show/route.ts`
  - `docs/sdd/v1.0/sectors/admin-reservations-schema-compat-gate.md`
  - `docs/sdd/v1.0/01-admin/admin-reservations.md`
  - `docs/sdd/v1.0/sectors/admin-reservations-hardening-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `/admin/reservations` -> 200
  - `/admin/reservations?status=PAID` -> 200
  - `/admin` -> 200
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - 레거시 reservations 스키마에서도 admin 예약 화면/통계가 즉시 깨지지 않는 상태 확보
  - 다음 단계는 reservations v1 마이그레이션으로 기능 완전성(NO_SHOW/COMPLETED) 복구

### 2026-02-20 기록 23 (Reservations v1 스키마 정합화 + 원격 적용 완료)
- 작업:
  - 원격 DB에서 `reservations.status` 부재 및 `cancellation_policies/settlements` 미생성 상태 확인
  - 보정 migration 작성 및 적용
    - 파일: `supabase/migrations/20260219133000_reservations_v1_schema_alignment.sql`
    - 적용 이름: `reservations_v1_schema_alignment_20260219`
  - `reservations` v1 핵심 컬럼/체크/인덱스 복구
  - `cancellation_policies` 기본 정책, `settlements` 테이블, `settlement_summary` 뷰 생성
  - no-show/admin 문서 게이트 상태 업데이트
- 변경 파일:
  - `supabase/migrations/20260219133000_reservations_v1_schema_alignment.sql`
  - `docs/sdd/v1.0/sectors/reservations-v1-schema-alignment-gate.md`
  - `docs/sdd/v1.0/sectors/admin-no-show-gate.md`
  - `docs/sdd/v1.0/01-admin/admin-no-show.md`
  - `docs/sdd/v1.0/01-admin/admin-reservations.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - DB: `reservations` 핵심 컬럼/제약 존재 확인
  - DB: `cancellation_policies`, `settlements`, `settlement_summary` 존재 확인
  - API: `GET /api/admin/no-show?date=2026-02-19` -> 200
  - API: `POST /api/admin/no-show` `{reservationId:"test-id"}` -> 400(예약 미존재)
  - Page: `/admin/no-show` 200, `/admin/settlements` 200
  - `npm run lint`, `npm run build` 통과
- 결과:
  - v1 admin 운영 블로커였던 reservations 스키마 불일치 해소
  - 다음 섹터는 admin/settlements 코드 경로 하드닝(권한가드 + client 경로 통일)

### 2026-02-20 기록 24 (Admin Settlements 하드닝 완료)
- 작업:
  - `admin/settlements` list/new/detail에 admin 진입 가드 적용
  - settlements actions의 전역 supabase client 제거
  - 페이지/액션 모두 admin optional client 우선 + server client fallback 경로로 통일
  - settlements SDD 문서(`admin-settlements.md`) 추가
- 변경 파일:
  - `app/admin/settlements/page.tsx`
  - `app/admin/settlements/new/page.tsx`
  - `app/admin/settlements/[id]/page.tsx`
  - `app/admin/settlements/actions.ts`
  - `docs/sdd/v1.0/sectors/admin-settlements-hardening-gate.md`
  - `docs/sdd/v1.0/01-admin/admin-settlements.md`
  - `docs/sdd/v1.0/01-admin/README.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `/admin/settlements` -> 200
  - `/admin/settlements/new` -> 200
  - `/admin/settlements/non-existent-id` -> 200(에러 대신 fallback 페이지)
  - `/admin/settlements?status=DRAFT` -> 200
  - `npm run lint` / `npm run build` 통과
- 결과:
  - 정산 섹터가 v1 admin 공통 보안/데이터 경로 규칙에 맞게 정렬됨
  - 다음 섹터는 settlements 상세 UUID 입력 검증 및 에러로그 정리

### 2026-02-20 기록 25 (Settlements UUID 입력 검증)
- 작업:
  - `app/admin/settlements/[id]/page.tsx`에 UUID 형식 검증 추가
  - `app/admin/settlements/actions.ts`의 `updateSettlementStatus`, `updateSettlementNotes`에 UUID 검증 추가
- 변경 파일:
  - `app/admin/settlements/[id]/page.tsx`
  - `app/admin/settlements/actions.ts`
  - `codex.md`
  - `합동작업 v1.md`
- 변경 내용:
  - `UUID_REGEX` + `isValidUUID()` 헬퍼 추가 (양 파일)
  - 페이지: 비UUID 입력 시 DB 쿼리 없이 "유효하지 않은 정산 ID" 화면 즉시 반환
  - 액션: 비UUID `settlement_id` 입력 시 400-수준 에러 응답 반환 (DB 에러 로그 차단)
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - `/admin/settlements/not-a-uuid` 접근 시 DB 에러 없이 안전한 fallback 페이지 렌더
  - `updateSettlementStatus/Notes` 호출 시 비UUID ID는 DB 도달 전 차단

### 2026-02-20 기록 26 (Settlements 권한 정합성 + RLS 하드닝)
- 작업:
  - settlements 액션 권한 불일치 수정: `CLUB_ADMIN`이 소속 클럽 정산을 `CONFIRMED`/메모 수정 가능하도록 정렬
  - `LOCKED` 전환은 `SUPER_ADMIN`만 허용하도록 서버 액션에서 강제
  - settlements 상세 페이지 액션 노출(`can_confirm/can_lock/can_edit`)을 실제 역할과 일치하도록 수정
  - `settlement_summary` + 외부 가격 크롤러 테이블 타입을 `types/database.ts`에 반영
  - 신규 RLS 보안 마이그레이션 추가 및 원격 DB 적용
- 변경 파일:
  - `app/admin/settlements/actions.ts`
  - `app/admin/settlements/[id]/page.tsx`
  - `types/database.ts`
  - `supabase/migrations/20260220143000_rls_hardening_settlements_reservations.sql`
  - `codex.md`
  - `합동작업 v1.md`
- 원격 DB 적용:
  - 적용 이름: `rls_hardening_settlements_reservations`
  - 적용 확인: `supabase_migrations.schema_migrations` 최신 버전 `20260220120421`
- 검증:
  - 로컬: `npm run lint` 통과
  - 로컬: `npm run build` 통과
  - DB: `reservations/settlements/cancellation_policies` RLS enabled = `true`
  - DB: 14개 신규 정책 생성 확인 (3개 테이블)
  - DB: `anon/authenticated` 과도 권한 회수 + 최소 권한 재부여 확인
- 결과:
  - settlements 섹터의 화면 권한/액션 권한 불일치 해소
  - 정산/예약/취소정책 테이블이 RLS 기반 최소권한 상태로 정렬됨

### 2026-02-20 기록 27 (Security Advisor 하드닝 마무리)
- 작업:
  - Supabase security advisor 경고 항목 기반 추가 하드닝 수행
  - 신규 migration 작성/적용:
    - `supabase/migrations/20260220152000_security_advisor_hardening.sql`
    - 적용 이름: `security_advisor_hardening`
  - 적용 내용:
    - `admin_user_stats`, `settlement_summary` 뷰 `security_invoker=true`로 전환
    - 9개 함수 `search_path=public` 고정
    - `golf_clubs`, `weather_cache` RLS 활성화 + public read/admin write 정책 추가
- 변경 파일:
  - `supabase/migrations/20260220152000_security_advisor_hardening.sql`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - Supabase Advisor(Security): 기존 ERROR/WARN(뷰/함수/RLS) 해소 확인
  - 잔여 경고: `auth_leaked_password_protection` 1건 (Auth 콘솔 설정 항목)
  - DB 검증:
    - `golf_clubs`, `weather_cache` RLS enabled = `true`
    - 두 뷰 `reloptions` = `security_invoker=true`
    - 9개 함수 `proconfig` = `search_path=public`
  - 로컬: `npm run lint`, `npm run build` 통과
- 결과:
  - 코드/DB 레벨로 처리 가능한 security advisor 항목은 모두 정리 완료
  - 남은 1건은 Supabase Auth 설정(운영 콘솔)에서 켜야 하는 정책성 옵션

### 2026-02-20 기록 28 (Admin 데이터 신선도 복구: 티타임/프라이싱 정상화)
- 작업:
  - 신규 섹터 게이트 작성: `docs/sdd/v1.0/sectors/admin-data-freshness-gate.md`
  - `seedCoreData`를 1회 초기 seed 방식에서 “미래 14일 top-up(idempotent)” 방식으로 개편
    - 누락된 `tee_times` 슬롯만 보충
    - 누락된 `weather_cache` 시간대만 보충
  - `tee_times` 스키마 정합성 반영:
    - `types/database.ts`에 `current_price`, `currency`, `created_at` 반영
    - 티타임 생성/가격수정 시 `current_price` 동기화
      - `app/admin/tee-times/actions.ts`
      - `app/api/admin/tee-times/route.ts`
      - `app/api/admin/tee-times/[id]/route.ts`
  - settings UI 문구를 “초기 데이터”에서 “데이터 보정”으로 변경
    - `app/admin/settings/page.tsx`
  - 원격 one-off 복구 SQL 실행(운영 데이터 즉시 복구)
- 변경 파일:
  - `docs/sdd/v1.0/sectors/admin-data-freshness-gate.md`
  - `app/admin/settings/actions.ts`
  - `app/admin/settings/page.tsx`
  - `types/database.ts`
  - `app/admin/tee-times/actions.ts`
  - `app/api/admin/tee-times/route.ts`
  - `app/api/admin/tee-times/[id]/route.ts`
  - `codex.md`
  - `합동작업 v1.md`
- 원격 복구 결과:
  - one-off insert: `+126 tee_times`, `+140 weather_cache`
  - `open_future_tee_times`: `0 -> 117`
  - `/api/pricing?limit=5`: 빈 배열에서 실제 데이터 반환으로 정상화 확인
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - “프라이싱이 안나오는” 핵심 원인이던 미래 OPEN 티타임 부재 상태 해소
  - settings 보정 액션을 통해 이후에도 동일 장애를 운영자 버튼으로 복구 가능

### 2026-02-20 기록 29 (Admin 대시보드 가시성 복구: 매출 차트/티타임 날짜 표기)
- 작업:
  - 신규 섹터 게이트 작성: `docs/sdd/v1.0/sectors/admin-dashboard-visibility-gate.md`
  - 대시보드 매출 차트 렌더 로직 조정:
    - `revenue.status=empty`에서도 최근 14일 막대 차트(0원 기준)를 유지 렌더
    - `revenue.status=error`만 차트 대신 오류 메시지 표시
    - `empty` 안내 문구를 차트 하단에 분리 노출
  - 대시보드 티타임 관리 탭 날짜/시간 표기 개선:
    - 컬럼명 `시간` -> `일시 (KST)`
    - 브라우저 로컬 타임존 대신 `Asia/Seoul` 고정 포맷 사용
- 변경 파일:
  - `components/AdminDashboardNew.tsx`
  - `docs/sdd/v1.0/01-admin/admin-dashboard.md`
  - `docs/sdd/v1.0/sectors/admin-dashboard-visibility-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - DB 확인: `reservations` 0건(최근 14일 0건) 상태 확인
  - 로컬: `npm run lint` 통과
  - 로컬: `npm run build` 통과
- 결과:
  - "일별 매출 추이가 안 보임" 체감 이슈를 empty 상태에서도 차트가 보이는 방식으로 해소
  - 티타임 관리 탭에서 날짜 포함 여부 혼동을 제거

### 2026-02-20 기록 30 (Crawler 우선 섹터 완료: Step 0 / 1-A / 1-B / 1-C)
- 작업:
  - 신규 섹터 게이트 작성/완료:
    - `docs/sdd/v1.0/sectors/crawler-step0-step1-gate.md`
  - Step 1-A 구현:
    - `POST /api/admin/crawler/run` 신규
    - 관리자 권한 또는 `x-cron-secret` 내부 호출 지원
    - `crawler/src/crawl-final-prices.mjs` 실행 후 `external_price_snapshots` 집계 반환
  - Step 1-B 구현:
    - `GET /api/cron/crawl-prices` 신규
    - `Authorization: Bearer CRON_SECRET` 검증
    - 루트 `vercel.json`에 4시간 cron 추가
  - Step 0 + 1-C 구현:
    - `/admin/crawler`에 URL 기반 FilterBar(지역/골프장/기간/상태) 추가
    - 선택 골프장 `SnapshotTable` 가격 히스토리 추가
    - 상단 `CrawlStatusPanel` + 수동 수집 실행 버튼/결과 카드 추가
- 변경 파일:
  - `app/admin/crawler/page.tsx`
  - `components/admin/CrawlerMonitorClient.tsx`
  - `app/api/admin/crawler/run/route.ts`
  - `app/api/cron/crawl-prices/route.ts`
  - `vercel.json`
  - `docs/sdd/v1.0/01-admin/admin-crawler.md`
  - `docs/sdd/v1.0/sectors/crawler-step0-step1-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과
  - `curl http://localhost:3010/admin/crawler` -> 200
  - `POST /api/admin/crawler/run` (`targetIds:[1]`) -> `success:true`, `total:1`, `noData:1`
  - `GET /api/cron/crawl-prices` -> `500 CRON_SECRET not configured` (미설정 보호 동작 확인)
- 결과:
  - 사용자가 지적한 미구현 4개 항목(Step0, 1-A, 1-B, 1-C) 코드 반영 완료
  - 남은 운영 작업: `.env.local`/Vercel에 `CRON_SECRET` 설정 후 cron 성공 경로 재검증

### 2026-02-20 기록 31 (Crawler 수집 실효성 검증 + 상태 가시성 강화)
- 작업:
  - 실제 크롤러 실행 검증:
    - `node crawler/src/crawl-final-prices.mjs --limit=3`
  - run API 응답 확장:
    - `authRequired` 카운트 추가
    - 사이트별 집계(`siteBreakdown`) 추가
  - CrawlerStatusPanel 강화:
    - 결과 카드에 `인증필요` 추가
    - 사이트별 실행 결과 테이블 추가
- 변경 파일:
  - `app/api/admin/crawler/run/route.ts`
  - `components/admin/CrawlerMonitorClient.tsx`
  - `docs/sdd/v1.0/sectors/crawler-step0-step1-gate.md`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - DB 최근 30분 집계:
    - `golfrock`: `29/29 AVAILABLE`
    - `golfpang`: `16/17 AVAILABLE`
    - `teeupnjoy`: `0/2 AVAILABLE` (NO_DATA)
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - “수집될 것 같지 않다” 상태에서 실제 AVAILABLE 수집이 들어오는 것을 데이터로 확인
  - 운영자가 사이트별 성공/데이터없음/인증필요를 한 번에 식별 가능

### 2026-02-20 기록 32 (teeupnjoy 크롤러 복구 검증 완료)
- 작업:
  - `crawler/src/crawl-final-prices.mjs`의 `crawlTeeupNjoyApi` 경로를 기준으로 teeup 단독 검증 실행
  - 드라이런 실행:
    - `node src/crawl-final-prices.mjs --target=1 --dry-run`
  - 실저장 실행:
    - `node src/crawl-final-prices.mjs --target=1`
  - DB 재확인 쿼리:
    - `external_price_snapshots` 최근 30분 `site_code/availability_status` 집계
    - teeup 최신 10건 상세 조회
- 변경 파일:
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - teeup 드라이런 결과: `rows: 16`, `failed: 0`
  - teeup 실저장 결과: `[1] Saved 16 row(s)`, `failed: 0`
  - DB 최근 30분 집계:
    - `golfrock`: `AVAILABLE 29`
    - `golfpang`: `AVAILABLE 16`, `NO_DATA 1`
    - `teeupnjoy`: `AVAILABLE 15`, `NO_DATA 3`
  - teeup 최신 적재 레코드에서 `final_price`, `tee_time`, `crawled_at` 정상 확인
- 결과:
  - `teeupnjoy`가 더 이상 실행 컨텍스트 오류로 중단되지 않고 실제 DB 적재까지 동작함을 확인
  - “수집이 안 될 것 같다” 우려에 대해 실행 로그 + DB 수치로 정상 수집 근거 확보

### 2026-02-20 기록 33 (Cron 성공 경로 실검증 + crawler 런타임 보강)
- 작업:
  - `app/api/cron/crawl-prices/route.ts` 수정:
    - 내부 run API 호출 base URL을 `request.nextUrl.origin` 우선 사용으로 변경
    - `localhost:3000` 고정 fallback으로 인한 로컬 호출 실패(`fetch failed`) 해소
  - `app/api/admin/crawler/run/route.ts` 수정:
    - target 조회 시 `site_code` 누락 선택 버그 수정(`siteBreakdown` 정확도 보장)
  - 런타임 검증:
    - dev(3010, `CRON_SECRET=local-cron-secret`)에서 cron 인증/미인증 호출
    - `/admin/crawler` 200
    - `/api/admin/crawler/snapshots` GET/PATCH 실호출
    - 메모/traits 저장 후 DB payload 반영 확인
- 변경 파일:
  - `app/api/cron/crawl-prices/route.ts`
  - `app/api/admin/crawler/run/route.ts`
  - `docs/sdd/v1.0/01-admin/admin-crawler.md`
  - `docs/sdd/v1.0/sectors/crawler-step0-step1-gate.md`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `GET /api/cron/crawl-prices` (no auth) -> `401 Unauthorized`
  - `GET /api/cron/crawl-prices` (Bearer) -> `200`
    - `total:5`, `succeeded:3`, `noData:2`, `failed:0`
    - `siteBreakdown`: `golfpang/golfrock/teeupnjoy` 성공, `golfmon/smartscore` noData
  - `POST /api/admin/crawler/run` (`targetIds:[1]`) -> `siteCode: teeupnjoy` 정상 집계 확인
  - `PATCH /api/admin/crawler/snapshots` (`snapshotId:227`) -> `manual_note(traits 포함)` 저장 성공
  - SQL 확인: `external_price_snapshots.id=227`의 `payload.manual_note` 반영 확인
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - Step 1-B cron 성공 경로를 로컬 런타임에서 실증했고, 운영 시나리오와 동일한 인증 흐름 확인 완료
  - Step 2(가격 옆 메모+특성 저장) 기능이 API/DB까지 정상 동작함을 검증 완료

### 2026-02-21 기록 34 (Supabase 로그인/관리자 권한 복구)
- 작업:
  - MCP로 Supabase 상태 재확인:
    - `get_project_url`, `get_publishable_keys`, `list_tables(public)` 정상 응답
  - 원인 확인:
    - Auth 로그 `/token`에서 `invalid_credentials` 다수 확인
    - `superadmin@tugol.dev`의 `public.users.is_admin/is_super_admin=false` 확인
  - 복구 적용(SQL):
    - `public.users` 관리자 플래그를 `true/true`로 복구
    - `auth.users` 비밀번호 해시 재설정(임시) + `updated_at` 갱신
- 변경 파일:
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `public.users` 조회:
    - `superadmin@tugol.dev` -> `is_admin=true`, `is_super_admin=true`
  - `auth.users` 조회:
    - `updated_at`이 복구 시각으로 갱신됨
- 결과:
  - 연결 단절이 아닌 인증/권한 불일치 이슈로 확정
  - 관리자 로그인 복구를 위한 계정 상태를 DB 기준으로 정상화 완료

### 2026-02-21 기록 35 (내 예약 상세 취소 모달 실연동)
- 작업:
  - `components/my/ReservationDetailClient.tsx` 취소 모달의 미구현 TODO 제거
  - `POST /api/reservations/cancel` 호출 로직 연결
    - payload: `reservationId`, `cancelReason: 'USER_REQUEST'`
  - 상태 처리 추가:
    - `isCancelling` 로딩 상태
    - `cancelError` 오류 상태
    - 처리 중 버튼 disable + 문구 변경
  - 성공/실패 UX:
    - 성공: 환불 예정 금액 안내 후 `/my/reservations` 이동 및 refresh
    - 실패: 모달 내 오류 문구 표시
- 변경 파일:
  - `components/my/ReservationDetailClient.tsx`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `npm run lint` 통과
- 결과:
  - `/my/reservations/[id]`에서 취소 버튼이 실제 취소 API 경로와 연결됨
  - 취소 진행 중/실패 상태가 사용자 화면에 표시되어 운영 신뢰도 개선

### 2026-02-21 기록 36 (예약 환불 처리 실연동)
- 작업:
  - `utils/cancellationPolicyV2.ts`
    - `processPaymentRefund()`를 Toss 환불 취소 API 호출로 구현
    - 설정/결제키 누락 및 HTTP 실패 응답 처리 보강
  - `app/api/reservations/cancel/route.ts`
    - `refundStatus`를 `pending/failed/not_required`로 구분 반환
    - `refundMessage` 필드 추가
    - `payment_key` 누락 시 실패 상태를 명시적으로 반환
- 변경 파일:
  - `utils/cancellationPolicyV2.ts`
  - `app/api/reservations/cancel/route.ts`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - 예약 취소 이후 환불 요청이 더미가 아닌 실제 PG 경로를 타도록 개선
  - 환불 상태가 API 응답에 명확히 노출되어 후속 UI/운영 추적이 쉬워짐

### 2026-02-21 기록 37 (teeup 기본 시드 IDs 정합성 보강)
- 작업:
  - `crawler/src/seed-default-targets.mjs`의 teeup 기본 `club_ids`를 임시 `[3]`에서 실제 discovery 값 14개로 교체
  - 반영 기준:
    - 원격 `external_price_targets(site_code='teeupnjoy')`의 `parser_config.club_ids`
- 변경 파일:
  - `crawler/src/seed-default-targets.mjs`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `npm --prefix crawler run check` 통과
- 결과:
  - 시드 스크립트만 실행해도 teeup 타겟이 운영에 맞는 club 범위로 생성되도록 정렬 완료

### 2026-02-21 기록 38 (tee_time_stats 휴일/KST 계산 보강)
- 작업:
  - `app/actions/sdd10-actions.ts`에 KST 기준 날짜 메타 헬퍼 추가
  - `aggregateTeeTimeStats`의 `day_of_week`, `hour_of_day`, `is_weekend`를 KST 기준으로 전환
  - `is_holiday` TODO 구현(대한민국 고정 공휴일 month-day 기준)
- 변경 파일:
  - `app/actions/sdd10-actions.ts`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - 집계 통계의 시간/요일/휴일 플래그가 서버 로컬 타임존 영향을 받지 않도록 정렬됨

### 2026-02-21 기록 39 (성능 하드닝 1: 미인덱스 FK 정리)
- 작업:
  - 미인덱스 FK 6건 확인 후 인덱스 추가 마이그레이션 작성/적용
  - 신규 파일:
    - `supabase/migrations/20260221113000_add_missing_fk_indexes.sql`
  - 원격 적용:
    - `add_missing_fk_indexes`
- 변경 파일:
  - `supabase/migrations/20260221113000_add_missing_fk_indexes.sql`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - 동일 FK 점검 SQL 재실행 결과 `[]` (미인덱스 FK 0건)
- 결과:
  - 성능 Advisor의 `unindexed_foreign_keys` 카테고리 대응 완료

### 2026-02-21 기록 40 (성능 하드닝 2: auth.uid initplan 최적화)
- 작업:
  - RLS 정책 9개를 `(select auth.uid())` 형태로 재작성하는 마이그레이션 작성/적용
  - 신규 파일:
    - `supabase/migrations/20260221114500_rls_auth_uid_initplan.sql`
  - 원격 적용:
    - `rls_auth_uid_initplan`
- 변경 파일:
  - `supabase/migrations/20260221114500_rls_auth_uid_initplan.sql`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `pg_policies` 검사에서 direct `auth.uid()` 사용 정책 0건 확인
- 결과:
  - `auth_rls_initplan` 성능 경고 원인 정책을 코드 기준으로 해소

### 2026-02-21 기록 41 (환경변수 템플릿 보강)
- 작업:
  - `.env.local.example`에 누락되어 있던 운영 키 템플릿 추가
    - `CRON_SECRET`
    - `GOLFROCK_LOGIN_ID`
    - `GOLFROCK_LOGIN_PW`
- 변경 파일:
  - `.env.local.example`
  - `합동작업 v1.md`
  - `codex.md`
- 결과:
  - 크론/크롤러 관련 운영 설정 누락 가능성을 템플릿 단계에서 감소

### 2026-02-21 기록 42 (Supabase 연결 재확인 + notifications INSERT 정책 중복 해소)
- 작업:
  - MCP로 Supabase 연결 상태 재확인(`now()`, DB 사용자/DB명 조회 성공)
  - `pg_policies` 점검 시 남아있던 permissive 중복 1건 정리
    - 대상: `public.notifications` / `INSERT`
  - 신규 마이그레이션 작성:
    - `supabase/migrations/20260221081027_consolidate_notifications_insert_policy.sql`
  - 원격 적용:
    - `consolidate_notifications_insert_policy`
- 변경 파일:
  - `supabase/migrations/20260221081027_consolidate_notifications_insert_policy.sql`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - 중복 permissive 정책 점검 SQL 결과 `[]`
  - `npm run lint` 통과
  - `npm run build` 통과
- 결과:
  - `notifications` INSERT가 단일 정책(`notifications_insert_admin_or_service_role`)으로 정리되어
    RLS 정책 중복 상태가 해소됨

### 2026-02-21 기록 43 (Crawler 정밀도 튜닝 1차)
- 작업:
  - `crawler/src/crawl-final-prices.mjs` 정밀도 보강
  - `golfmon/smartscore`:
    - 앱 인증 필요 소스를 window별 `AUTH_REQUIRED`로 명시 저장하도록 수정
    - 후처리 기본 `IMMINENT_3H NO_DATA`로 덮이는 오분류 케이스 방지
  - `golfpang/golfrock/teeupnjoy`:
    - `IMMINENT_3H`는 실제 3시간 이내 티타임만 포함하도록 필터 강화
  - `teeupnjoy`:
    - 가격 파싱 fallback 확장(`bookDiscount` 외 `bookPrice/salePrice/normalPrice/bookNormal/price`)
    - payload에 `price_source` 기록
- 변경 파일:
  - `crawler/src/crawl-final-prices.mjs`
  - `codex.md`
  - `합동작업 v1.md`
- 검증:
  - `npm --prefix crawler run check` 통과
  - 로컬 dry-run은 sandbox 네트워크 제약으로 `fetch failed`(실DB 통신 불가)
- 기대 결과:
  - 사이트별 상태 분류 정확도 향상(AUTH_REQUIRED/NO_DATA 구분 개선)
  - 임박 창 데이터의 노이즈 감소

### 2026-02-21 기록 44 (Crawler 정밀도 튜닝 2차)
- 작업:
  - `crawler/src/crawl-final-prices.mjs` 정밀도 2차 보강
  - 공통 window 정합 함수(`matchesWindowByPlayDate`) 추가
    - 실제 `play_date` 기준으로 `WEEK_BEFORE/TWO_DAYS_BEFORE/SAME_DAY_MORNING/IMMINENT_3H` 강제 일치
  - 날짜 파서 확장
    - `parseMonthDayToDate`: `MM/DD` + `MM월DD일` 모두 지원
  - 사이트별 반영
    - `golfpang`: `bookingDateText` 기준 실제 날짜 파싱 + window 검증
    - `golfrock`: AJAX/양도 테이블 모두 실제 날짜 기준 검증
    - `teeupnjoy`: `bookingTime` zero-padding 파싱 보강 + window 검증
  - 중복 노이즈 완화
    - `dedupeAvailableRows` 추가 후 day-part 샘플링 수행
- 변경 파일:
  - `crawler/src/crawl-final-prices.mjs`
  - `합동작업 v1.md`
  - `codex.md`
- 검증:
  - `npm --prefix crawler run check` 통과
  - dry-run:
    - `--target=4/5 --window=IMMINENT_3H` -> `AUTH_REQUIRED` 정상
    - `--target=2 --window=WEEK_BEFORE` -> 9행(오분류 제거)
    - `--target=3 --window=WEEK_BEFORE` -> 8행(중복 완화)
- 기대 결과:
  - 사이트별 window 정밀도 향상
  - 골프락 양도 섹션의 window 누수 감소
  - 샘플 결과의 중복 노이즈 감소
